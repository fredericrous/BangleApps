g.clear();
g.setFontAlign(0,0); // center font
g.setFont("Vector",20); // vector font, 80px  
// draw the current counter value
g.drawString("Tandem Tales",120,120);
// optional - this keeps the watch LCD lit up
g.flip();
